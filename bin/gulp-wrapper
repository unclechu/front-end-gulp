#!/usr/bin/env node

var path = require('path');

process.argv.push('--gulpfile=' + path.join(path.dirname(module.filename), '..', 'gulpfile.js'));
process.argv.push('--cwd=' + path.join(path.dirname(module.filename), '..', '..', '..'));

require('gulp/bin/gulp');
